For my PA3 you need to compile using gcc -Wall pa3.c -o pa3 and then ./pa3 processes.txt 2 provided by the pa3.docx and for my specific code and macOS I put the input into a sticky note and then saved it as a txt file. To compile and run my code it can be ran in the terminal of a macOS or in windows. When running gcc -Wall -o pa3 pa3.c it doesn't print out anything but if it does not compile it instead of giving a new line would have the warnings and why it is not executable. I have my program and txt file both on my desktop so it is easy to access all of the information. All the code is in one file and for the example input and outputs I just used the provided one in the documents and most of the results matched with what should have been produced. 
